<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- release 包必须显式声明联网权限：Flutter 模板只在 debug/profile 的 manifest 里加了它，
         缺少这一行时正式版 APK 无法发起任何 HTTP 请求（登录会直接失败）。 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 常驻通知：Android 13(API 33) 起发通知需要运行时授权，由「我的」页面的开关触发申请。
         后台刷新用 WorkManager 完成，不需要前台服务权限，因此不受 Android 15 起
         dataSync 前台服务「每 24 小时最多 6 小时」的限制。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 重启后重新安排通知刷新（AlarmManager 的闹钟在重启后会丢失） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="福瑞记账"
        android:usesCleartextTraffic="true"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 常驻通知的后台刷新：AlarmManager 定时触发，不对外暴露 -->
        <receiver
            android:name=".LedgerRefreshReceiver"
            android:exported="false" />

        <!-- 开机后重新安排刷新；只接收系统广播 -->
        <receiver
            android:name=".LedgerBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
